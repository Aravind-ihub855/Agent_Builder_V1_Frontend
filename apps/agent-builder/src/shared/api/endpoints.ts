/**
 * Shared API Endpoints Wrapper
 * Re-exports all feature endpoints for centralized access
 */
export { AUTH_ENDPOINTS } from '@/features/auth/api/endpoints';
export { DASHBOARD_ENDPOINTS } from '@/features/dashboard/api/endpoints';
